import 'package:flutter/material.dart';

class SnipnetLearn extends StatefulWidget {
  const SnipnetLearn({Key? key}) : super(key: key);
  @override
  State<SnipnetLearn> createState() => _SnipnetLearnState();
}

class _SnipnetLearnState extends State<SnipnetLearn> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
